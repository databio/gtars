% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R, R/sc.R
\name{sc_run_rna_pipeline}
\alias{sc_run_rna_pipeline}
\title{Run the full RNA preprocessing pipeline}
\usage{
sc_run_rna_pipeline(
  ptr,
  min_features = 200L,
  min_cells = 3L,
  max_pct_mt = 5,
  scale_factor = 10000,
  n_variable_features = 2000L,
  n_pcs = 50L,
  clip_value = 10
)

sc_run_rna_pipeline(
  ptr,
  min_features = 200L,
  min_cells = 3L,
  max_pct_mt = 5,
  scale_factor = 10000,
  n_variable_features = 2000L,
  n_pcs = 50L,
  clip_value = 10
)
}
\arguments{
\item{ptr}{External pointer to a FeatureMatrix}

\item{min_features}{Minimum features per cell (default 200)}

\item{min_cells}{Minimum cells per gene (default 3)}

\item{max_pct_mt}{Maximum mitochondrial percentage (default 5.0)}

\item{scale_factor}{Normalization scale factor (default 10000)}

\item{n_variable_features}{Number of HVGs (default 2000)}

\item{n_pcs}{Number of PCs (default 50)}

\item{clip_value}{Clip value for scaling (default 10.0, NA to skip)}
}
\value{
A named list with embeddings, variance_explained, variable_features,
and cell metadata
}
\description{
Run the full RNA preprocessing pipeline

Run full RNA preprocessing pipeline
}
