% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R, R/sc.R
\name{sc_run_full_pipeline}
\alias{sc_run_full_pipeline}
\title{Run the full RNA analysis pipeline (preprocessing + clustering + markers)}
\usage{
sc_run_full_pipeline(
  ptr,
  min_features = 200L,
  min_cells = 3L,
  max_pct_mt = 5,
  scale_factor = 10000,
  n_variable_features = 2000L,
  n_pcs = 50L,
  clip_value = 10,
  k_neighbors = 20L,
  resolution = 0.8,
  compute_markers = TRUE,
  compute_silhouette = TRUE
)

sc_run_full_pipeline(
  ptr,
  min_features = 200L,
  min_cells = 3L,
  max_pct_mt = 5,
  scale_factor = 10000,
  n_variable_features = 2000L,
  n_pcs = 50L,
  clip_value = 10,
  k_neighbors = 20L,
  resolution = 0.8,
  compute_markers = TRUE,
  compute_silhouette = TRUE
)
}
\arguments{
\item{ptr}{External pointer to a FeatureMatrix}

\item{min_features}{Minimum features per cell (default 200)}

\item{min_cells}{Minimum cells per gene (default 3)}

\item{max_pct_mt}{Maximum mitochondrial percentage (default 5.0)}

\item{scale_factor}{Normalization scale factor (default 10000)}

\item{n_variable_features}{Number of HVGs (default 2000)}

\item{n_pcs}{Number of PCs (default 50)}

\item{clip_value}{Clip value for scaling (default 10.0, NA to skip)}

\item{k_neighbors}{Number of nearest neighbors (default 20)}

\item{resolution}{Leiden resolution (default 0.8)}

\item{compute_markers}{Compute marker genes (default TRUE)}

\item{compute_silhouette}{Compute silhouette scores (default TRUE)}
}
\value{
A named list with embeddings, clusters, markers, and cell metadata
}
\description{
Run the full RNA analysis pipeline (preprocessing + clustering + markers)

Run the full RNA analysis pipeline (preprocessing + clustering + markers)
}
