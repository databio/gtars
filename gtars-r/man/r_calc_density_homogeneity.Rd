% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{r_calc_density_homogeneity}
\alias{r_calc_density_homogeneity}
\title{Summary statistics over the dense per-window count vector.}
\usage{
r_calc_density_homogeneity(rs_ptr, n_bins, chrom_names, chrom_sizes_vec)
}
\arguments{
\item{rs_ptr}{External pointer to a RegionSet}

\item{n_bins}{Target bin count for the longest chromosome — not the
total number of bins returned. See \code{r_calc_density_vector}.}

\item{chrom_names}{Character vector of chromosome names}

\item{chrom_sizes_vec}{Integer vector of chromosome sizes}
}
\description{
Returns mean, population variance, coefficient of variation, Gini
coefficient, and the count of nonzero windows. Gini is biased high
for very sparse count distributions; inspect \code{n_nonzero_windows}
before interpreting Gini on sparse peak sets.
}
\details{
\code{n_bins} is the target bin count for the longest chromosome (see
\code{r_calc_density_vector} for the full semantic), not the total window
count. Short contigs in \code{chrom_sizes} each contribute a narrow
single-bin entry which dilutes \code{mean_count}, inflates \code{n_windows},
and raises \code{gini}.
}
