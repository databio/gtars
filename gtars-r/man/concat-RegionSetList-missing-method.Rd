% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RegionSetList-class.R
\name{concat,RegionSetList,missing-method}
\alias{concat,RegionSetList,missing-method}
\title{Flatten a RegionSetList into a single RegionSet}
\usage{
\S4method{concat}{RegionSetList,missing}(x, y)
}
\arguments{
\item{x}{A RegionSetList object}

\item{y}{missing (not used for RegionSetList)}
}
\value{
A \code{RegionSet} object
}
\description{
Flatten all region sets into a single RegionSet without
merging or deduplication. Apply \code{disjoin()} or \code{reduce()}
on the result as needed.

When called as \code{concat(rsl)} (single argument), flattens the list.
The generic \code{concat(x, y)} is defined in RegionSet-methods.R.
}
