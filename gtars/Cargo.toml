[package]
name = "gtars"
version = "0.6.1"
edition = "2024"
description = "Performance critical tools for genomic interval analysis."
homepage = "https://github.com/databio/gtars"
repository = "https://github.com/databio/gtars"
documentation = "https://docs.rs/gtars"
readme = "../README.md"
license = "MIT"
categories = ["science::bioinformatics::genomics", "science::bioinformatics"]


[dependencies]
gtars-core = { path = "../gtars-core", optional = true, version="0.5.4", features = ["bigbed"] }
gtars-tokenizers = { path = "../gtars-tokenizers", features = ["huggingface"], optional = true, version="0.5.2" }
gtars-io = { path = "../gtars-io", optional = true, version="0.5.0" }
gtars-refget = { path = "../gtars-refget", optional = true, version="0.7.0" }
gtars-overlaprs = { path = "../gtars-overlaprs", optional = true, version="0.5.1" }
gtars-uniwig = { path = "../gtars-uniwig", optional = true, version="0.5.0" }
gtars-igd = { path = "../gtars-igd", optional = true, version="0.5.0" }
gtars-bbcache = { path = "../gtars-bbcache", optional = true, version="0.5.2" }
gtars-scoring = { path = "../gtars-scoring", optional = true, version="0.5.1" }
gtars-fragsplit = { path = "../gtars-fragsplit", optional = true, version="0.5.0" }


[features]
default = []

core = ["dep:gtars-core"]
tokenizers = ["dep:gtars-tokenizers"]
io = ["dep:gtars-io"]
refget = ["dep:gtars-refget"]
overlaprs = ["dep:gtars-overlaprs"]
uniwig = ["dep:gtars-uniwig"]
igd = ["dep:gtars-igd"]
bbcache = ["dep:gtars-bbcache"]
scoring = ["dep:gtars-scoring"]
fragsplit = ["dep:gtars-fragsplit"]

[package.metadata.docs.rs]
all-features = true